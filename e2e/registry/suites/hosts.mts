// Preserve the registry's category-owned import surface while descriptor-only
// Playwright configs consume the cycle-free support owner directly.
export { HOST_E2E_SUITES } from '../../support/host-suites.mts';
