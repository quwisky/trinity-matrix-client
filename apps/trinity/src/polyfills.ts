/**
 * Polyfills loaded before the app.
 *
 * zone.js is not imported — the app runs zoneless under
 * `provideZonelessChangeDetection()` (see apps/trinity/src/main.ts and
 * apps/docs-developers/src/content/docs/architecture/state-and-reactivity.md). All targeted browsers are evergreen, so no other polyfills
 * are currently needed. Add browser polyfills here if a target requires one.
 */
