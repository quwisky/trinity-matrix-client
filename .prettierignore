# Dependencies
node_modules

# Build output
www
dist
.nx
.angular
coverage

# Capacitor native projects (managed by native toolchains)
android
ios

# Generated / binary
pnpm-lock.yaml
*.wasm

# Agent skills + tool-managed config (vendored / generated — don't reformat)
.agents
.claude
CLAUDE.md
skills-lock.json

# Canonical @spartan-ng/cli-generated helm directives — leave them in the
# generator's own class order so a re-sync doesn't fight prettier-plugin-tailwindcss.
# (Only the generated hlm-*.ts; the hand-authored trn-* overlay adapters stay formatted.)
libs/spartan/**/hlm-*.ts

.pnpm-store/
**/.pnpm-store/
