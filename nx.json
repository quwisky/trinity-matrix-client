{
  "$schema": "./node_modules/nx/schemas/nx-schema.json",
  "defaultBase": "develop",
  "namedInputs": {
    "default": ["{projectRoot}/**/*", "sharedGlobals"],
    "production": [
      "default",
      "!{projectRoot}/**/?(*.)+(spec|test).[jt]s?(x)?(.snap)",
      "!{projectRoot}/tsconfig.spec.json",
      "!{projectRoot}/vite.config.ts",
      "!{projectRoot}/src/test-setup.[jt]s",
      "!{projectRoot}/**/*.stories.@(js|jsx|ts|tsx|mdx)",
      "!{projectRoot}/.storybook/**/*"
    ],
    "sharedGlobals": [
      "{workspaceRoot}/tsconfig.base.json",
      "{workspaceRoot}/.postcssrc.json"
    ]
  },
  "targetDefaults": {
    "build": {
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["production", "^production"]
    },
    "test": {
      "cache": true,
      "inputs": ["default", "^production"],
      "//": "Memory is bounded by the vitest pool, not from here — see the `pool` comment in vite.base.config.ts. This used to set VITEST_MAX_THREADS=2 to cap each worker pool; that was aimed at the wrong thing (a CPU invariant for a memory-bound failure), it held while the suite still failed, and under the `forks` pool the variable is a no-op anyway since it only writes poolOptions.threads/vmThreads.",
      "options": {
        "command": "vitest run",
        "cwd": "{projectRoot}"
      },
      "executor": "nx:run-commands",
      "configurations": {
        "watch": {
          "command": "vitest"
        }
      }
    },
    "typecheck": {
      "cache": true,
      "//": "The ONLY gate that type-checks specs. Every project's tsconfig.json excludes `src/**/*.spec.ts`, the type-aware ESLint block ignores them too, and Vitest goes through @analogjs/vite-plugin-angular, which transpiles without checking — so until this existed a spec could drift arbitrarily far from the code it claims to test (a fake client missing 358 MatrixClient members, a mock tuple the assertions index past the end of) and every gate stayed green. `^production` for the same reason lint needs it: a spec's types come from its DEPENDENCIES' source, so a model change in data-access must invalidate its consumers' cached pass.",
      "inputs": ["default", "^production"],
      "options": {
        "command": "tsc -p tsconfig.spec.json --noEmit",
        "cwd": "{projectRoot}"
      },
      "executor": "nx:run-commands"
    },
    "lint": {
      "cache": true,
      "//": "`^production` is load-bearing: @typescript-eslint/no-deprecated is type-aware, so a project's lint result depends on its DEPENDENCIES' source (the @deprecated tag lives there). Without dependency inputs, deprecating an export in util-matrix leaves every consumer reporting a cached pass — the ban silently stops working across exactly the lib boundaries it exists to police.",
      "inputs": ["default", "^production", "{workspaceRoot}/eslint.config.mjs"]
    },
    "@nx/angular:ng-packagr-lite": {
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["production", "^production"]
    }
  },
  "plugins": [
    {
      "plugin": "@nx/eslint/plugin",
      "options": {
        "targetName": "lint"
      }
    },
    {
      "plugin": "@nx/playwright/plugin",
      "options": {
        "targetName": "e2e"
      }
    },
    {
      "plugin": "@nx/storybook/plugin",
      "options": {
        "serveStorybookTargetName": "storybook",
        "buildStorybookTargetName": "build-storybook",
        "testStorybookTargetName": "test-storybook",
        "staticStorybookTargetName": "static-storybook",
        "buildDepsTargetName": "build-deps",
        "watchDepsTargetName": "watch-deps"
      }
    }
  ],
  "analytics": false,
  "generators": {
    "@nx/angular:library": {
      "linter": "eslint",
      "unitTestRunner": "none"
    },
    "@nx/angular:component": {
      "style": "css"
    }
  }
}
