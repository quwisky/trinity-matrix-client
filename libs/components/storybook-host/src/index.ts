/**
 * Intentionally empty.
 *
 * This project is Storybook configuration, not a component library — `.storybook/` is the
 * whole of it. The entry point exists so the workspace's own invariants apply here uniformly
 * (`scripts/lint-invariants.spec.mjs` probes every library's TypeScript to prove the
 * `matrix-js-sdk` ban reaches it), rather than this project being a hole in them.
 */
export {};
