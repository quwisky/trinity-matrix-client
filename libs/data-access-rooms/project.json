{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "data-access-rooms",
  "projectType": "library",
  "sourceRoot": "libs/data-access-rooms/src",
  "prefix": "trn",
  "tags": ["type:data-access", "scope:trinity"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run", "cwd": "libs/data-access-rooms" },
      "configurations": { "watch": { "command": "vitest" } }
    }
  }
}
