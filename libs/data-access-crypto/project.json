{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "data-access-crypto",
  "projectType": "library",
  "sourceRoot": "libs/data-access-crypto/src",
  "prefix": "trn",
  "tags": ["type:data-access", "scope:trinity"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run", "cwd": "libs/data-access-crypto" },
      "configurations": { "watch": { "command": "vitest" } }
    }
  }
}
