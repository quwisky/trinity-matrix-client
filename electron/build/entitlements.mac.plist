<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the signed/notarized macOS build.

  Electron embeds V8 + WASM (matrix-sdk-crypto), which JITs executable memory, so
  under the hardened runtime it needs the JIT / unsigned-executable-memory /
  disable-library-validation exceptions or it crashes on launch once signed.
  network.client lets the renderer reach arbitrary Matrix homeservers over
  HTTPS/WSS. Referenced from electron-builder.yml (`mac.entitlements` +
  `mac.entitlementsInherit`); only applied when a Developer ID identity is used.
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.device.camera</key>
    <true/>
  </dict>
</plist>
